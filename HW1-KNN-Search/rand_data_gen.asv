C = [1, 2, 3; 4, 5, 6; 7, 8, 9];
Q = [3, 2, 1];

save('test/data.mat', 'C', 'Q');

[Idx, D] = knnsearch(C, Q, 'K', 3);

% Display the distance matrix
disp('Distance Matrix D:');
disp(D);

disp('Index matrix Idx:');
disp(Idx);